{
  "name": "vrt-poc",
  "version": "1.0.0",
  "description": "PoC of using VRT integrated with Cypress.io and sorry-cypress",
  "files": [
    "dist/*"
  ],
  "directories": {
    "dist": "dist"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "./node_modules/.bin/cypress open",
    "build": "npm i && npm run clean-target && tsc && npm run prepare-resources",
    "clean-target": "del-cli --force ./dist",
    "prepare-resources": "mkdir dist; cp cypress.json dist/cypress.json && ln -s $(pwd)/node_modules dist/node_modules",
    "debug": "./node_modules/.bin/cypress open",
    "postinstall": "patch-package"
  },
  "author": "Francisco Fuentes Barrera",
  "license": "ISC",
  "devDependencies": {
    "@visual-regression-tracker/agent-cypress": "^4.8.1",
    "@visual-regression-tracker/sdk-js": "4.7.0",
    "cy2": "^1.0.4",
    "cypress": "^7.3.0",
    "del-cli": "^3.0.1",
    "patch-package": "^6.4.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4",
    "webpack": "^4.46.0"
  }
}
